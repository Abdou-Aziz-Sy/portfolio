# Passation — portfolio, 17 septembre 2026

## Où en est le projet

- Dépôt : `C:\Users\syabd\Documents\Projects\Perso\portfolio`, branche **`feat/v1`**, dernier commit `8883a8e`. `main` ne contient que la spec et le plan.
- Spec : `docs/superpowers/specs/2026-09-17-portfolio-design.md`. Plan : `docs/superpowers/plans/2026-09-17-portfolio.md`.
- **Tâches 1 à 12 du plan terminées** : squelette, jetons et CSS portés, icônes, couche de contenu, gabarit, accueil, projets filtrables, études de cas MDX (4 projets), blog, À propos, SEO (sitemap, robots, Open Graph, JSON-LD, 404), tests e2e, CI, README.
- **Tâche 13 en cours** (vérification finale et déploiement).

### Vérifications au 17/09

| Contrôle | Résultat |
|---|---|
| `pnpm lint`, `pnpm typecheck`, `pnpm build` | OK |
| `pnpm test` | 26 tests OK |
| `pnpm test:e2e` | 35 OK, 1 ignoré (menu mobile sur le projet bureau) |
| Revue visuelle (bureau, mobile, sombre, clair) | OK |
| Lighthouse mobile local | accessibilité, bonnes pratiques et SEO à 100 ; performance de 85 à 93, très bruitée en local ; CLS 0 |

## Reste à faire, dans l'ordre

1. **Corriger le CV avant publication.** `public/cv.pdf` est une copie de `C:\Users\syabd\Documents\Docs pro\cv\CV_Abdou_Aziz_SY.pdf`, qui contient une affirmation fausse :
   - `CV_Abdou_Aziz.tex` ligne 153 et `CV_Abdou_Aziz_v2.tex` ligne 244 : « Suppression de la ressaisie manuelle des dossiers de candidature par OCR… ».
   - En réalité, l'OCR + LLM sert à **pré-remplir la création d'un appel à candidature** à partir de son document officiel (`backend/controllers/callController.js`, route `/calls/extract`). Aucune extraction ne porte sur les dossiers des candidats.
   - Formulation proposée : « **Pré-remplissage automatique des appels à candidature** à partir de leur document officiel, par OCR et modèle de langage auto-hébergé ; documents officiels générés, signés et archivés automatiquement. »
   - Recompiler les deux versions (`pdflatex -jobname=CV_Abdou_Aziz_SY CV_Abdou_Aziz.tex` et `-jobname=CV_Abdou_Aziz_SY_v2 CV_Abdou_Aziz_v2.tex`), vérifier qu'elles tiennent sur une page, puis recopier la V1 dans `public/cv.pdf`.
2. **Corriger l'atelier LinkedIn** (artefact https://claude.ai/artifact/Q73ajWVyUuGWxyDGgteeS3) avec la même correction, et remplacer « exercice de restauration vérifié » par « exercice de restauration automatisé » : aucun procès-verbal de restauration n'existe dans UGB Link, seul l'outil `backend/database/restore-test.sh` existe.
3. **Finaliser la branche** (skill superpowers:finishing-a-development-branch) : relancer toute la chaîne, fusionner `feat/v1` dans `main`.
4. **Créer le dépôt public** `Abdou-Aziz-Sy/portfolio` et pousser. Vérifier d'abord que `.work/` n'est pas suivi. `gh` est authentifié ; en cas d'échec SSH, pousser en HTTPS.
5. **Déployer sur Vercel** : projet `portfolio`, production depuis `main`. Le CLI `vercel` (v59) est disponible, mais son authentification n'est pas vérifiée ; des outils Vercel MCP existent aussi. Sans authentification, donner à l'utilisateur la procédure d'import du dépôt dans Vercel.
6. **Mesurer Lighthouse sur l'URL de production.** Objectif de la spec : 95 ou plus. En local, le LCP simulé plafonne vers 3,2 s, avec un LCP réel d'environ 0,3 s.
7. Mettre à jour la mémoire du projet, puis présenter à l'utilisateur les observations task-observer n° 34 à 37 de cette session.

## En attente de l'utilisateur (les sections restent masquées tant qu'il manque)

- Portrait : `public/portrait.jpg`, puis `portrait: "/portrait.jpg"` dans `content/site.ts`.
- URL LinkedIn : `site.liens.linkedin`.
- Captures d'UGB Link, prises sur le seed de démo, jamais sur la production.
- Relecture des 3 articles (`content/blog/*.mdx`, actuellement `publie: false`). Aperçu local : `AFFICHER_BROUILLONS=1 pnpm build && pnpm start`.
- Recommandations réelles (`site.recommandations`).
- Sources des autres projets à ajouter.

## Décisions et écarts par rapport au plan

- **Styles** : `styles/tokens.css` et `styles/plan.css` sont générés (`pnpm tokens`, `pnpm port-css`) ; les ajouts vivent dans `styles/ajouts.css`, jamais régénéré.
- **Navigation** : le lien actif est calculé par `usePathname`. Le bouton de menu mobile garde le libellé « Menu », son état étant porté par `aria-expanded`.
- **Types** : `pnpm typecheck` lance `next typegen` avant `tsc`, car Next 16 génère les types `PageProps`.
- **Playwright** : Edge (`channel: msedge`) en local, faute d'avoir pu télécharger Chromium (délai dépassé) ; Chromium en CI.
- **Aperçu des brouillons** : `AFFICHER_BROUILLONS=1`, ignoré quand `VERCEL_ENV=production`.
- **Performance** : l'animation `pa-rise` part d'une opacité de 0,35 (jamais 0) ; les trois polices sont préchargées ; l'icône est `app/icon.svg`. `content-visibility` a été essayé puis retiré (aucun gain).
- **Image Open Graph** : police par défaut du générateur. Amélioration possible : embarquer un fichier TTF de Schibsted Grotesk.
- **Corrections de faits UGB Link**, vérifiées dans le code et appliquées au site :
  - extraction = appels à candidature ;
  - modèle en production `qwen2.5:1.5b` ; VPS de 6 vCPU et 12 Go, sans GPU ;
  - SSE : `X-Accel-Buffering: no` et ping toutes les 25 s ; bus en mémoire filtré par audience, personne par défaut ;
  - sauvegardes : outil d'exercice de restauration, sans procès-verbal ;
  - CI : build, déploiement SSH et test de fumée, sans la suite e2e.

## Outils locaux (non versionnés, dans `.work/`)

- `bash .work/serve.sh` : relance `next start` sur le port 3001. **Toujours arrêter ce serveur avant un `pnpm build`.**
- `node .work/slices.mjs <url> <prefixe> <largeur> <theme> <hauteur>` : captures par tranches.
- `node .work/el.mjs <url> <selecteur> <sortie> <largeur> <theme>` : capture d'un seul élément.
- `bash .work/lh.sh <chemin> <n>` : n mesures Lighthouse.
- Sous Git Bash, préfixer par `MSYS_NO_PATHCONV=1` toute commande qui reçoit un chemin commençant par `/`.
